{
  "name": "jobpilot-apply-worker",
  "version": "0.1.0",
  "private": true,
  "description": "Playwright auto-apply worker for JobPilot (BullMQ consumer)",
  "license": "UNLICENSED",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "start:dev": "tsx watch src/index.ts",
    "lint": "tsc -p tsconfig.json --noEmit",
    "test": "node --test --import tsx src/**/*.test.ts"
  },
  "dependencies": {
    "bullmq": "^5.79.1",
    "dotenv": "^16.4.7",
    "ioredis": "^5.11.1",
    "mongodb": "^6.12.0",
    "playwright": "^1.50.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.5",
    "tsx": "^4.19.2",
    "typescript": "^5.7.3"
  }
}
